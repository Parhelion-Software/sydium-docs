---
title: Authentication
---

Authenticate with an opaque API key sent as `Authorization: Bearer sk_live_...` (prod) or `sk_test_...` (staging). Keys are scoped (e.g. `posts:write`, `analytics:read`); a key can never exceed the issuing member's team role.
