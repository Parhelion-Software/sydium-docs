---
title: Authentication
---

Authenticate with an opaque API key sent as `Authorization: Bearer sk_live_...`. Keys are scoped (e.g. `posts:write`, `analytics:read`); a key can never exceed the issuing member's team role.
